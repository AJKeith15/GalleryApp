import React from 'react';

// component for when no pics are found in search
const NoPics = () => (
    <li className='not-found'>
        <h3>No Results Found</h3>
        <p>Your search did not return any results. Please try again.</p>
    </li>
);

export default NoPics;